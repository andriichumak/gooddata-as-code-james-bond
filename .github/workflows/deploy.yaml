name: Deploy to GoodData server

on:
  push:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: '20.x'
      - run: npm ci
      # Enable validate step after v0.4.1 is out
      # - run: npm run validate
      - run: npm run deploy
        env:
          GOODDATA_API_TOKEN: ${{ secrets.GD_TOKEN }}
